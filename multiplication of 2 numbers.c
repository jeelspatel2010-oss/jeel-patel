#include<stdio.h>

  int main()
  {
  int a, b;

  printf("Enter 2 numbers for multiplication\n");
  scanf("%d %d",&a, &b);

  printf("multiplication of %d and %d is %d\n", a, b, (a*b));

  return 0;
  }
