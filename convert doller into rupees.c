#include<stdio.h>

int main ()
{
   float doller,inr;

   printf("Enter the amount in doller:-");
   scanf("%f",&doller);

   inr=doller*48;

   printf("Amount in INR is:=%f",inr);
   return 0;
   }
